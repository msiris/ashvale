# CLAUDE.md

이 파일은 매 세션 자동으로 읽힌다. 아래 규칙은 **모든 작업에 항상 적용된다.**

## 프로젝트

「아쉬베일 연대기」 — 탑다운 픽셀 필드를 걸어다니는, 끝나지 않는 관계 중심 RPG.
안드로이드 세로 화면 전용 웹 앱(PWA). 완전 무서버, 완전 오프라인.

**몬스터 수집은 없다.** 잡아 모으거나 도감을 채우는 구조가 아니다.
전투는 **마주섬** 하나뿐이다 — 이야기의 마지막 판과 지역에서 만나는 것.
체력을 두고 겨루되 기색을 읽고 자세로 맞선다. 때리는 버튼을 연타하는 자리가 아니고,
여럿과 연달아 맞서지도 않는다. 한 번에 하나다.

스택: Vite + React 18 + TypeScript + Tailwind + Phaser 3 + zustand

## 절대 규칙

### 문서 읽기
- `docs/game-design-doc.md`는 길다. **전체를 읽지 마라.** 지시받은 절만 읽는다
- 지시에 절 번호가 없으면 물어라. 추측해서 전체를 읽지 마라

### 범위
- **현재 마일스톤 범위를 넘어서 구현하지 마라.** 다음 마일스톤의 자리를 미리 비워두는 것도 하지 마라
- 마일스톤이 끝나면 완료 조건 충족 여부를 보고하고 **멈춘다.** "다음 것도 할까요"라고 묻지 말고 그냥 멈춘다

### 아키텍처
- 규칙 로직은 `src/systems/`의 **순수 함수**: `(state, input, rng) => { state, entries }`
- React 컴포넌트나 Phaser 씬에 게임 규칙을 넣지 마라
- Phaser 씬은 `GameState`를 **읽기만** 한다. `syncFromState(state)` 한 곳에서 갱신
- 입력은 Phaser → 콜백 → zustand 액션 순서로만 흐른다
- 수치는 전부 `src/data/*.ts` 상수로. 코드에 숫자를 박지 마라
- **시각 상태를 저장하지 마라.** 예외는 `world.heroTile` 하나 — 어디에 서 있는지는 게임 상태다

### 저장
- `localStorage` / `indexedDB`를 **`StorageAdapter` 밖에서 직접 호출하지 마라**
- 원장(ledger)은 세이브와 별도 키다. 불러오기가 값을 낮추지 못한다
- 서비스 워커에서 `caches.delete()`는 써도 되지만 IndexedDB/localStorage는 **절대** 건드리지 마라
- 마이그레이션 실패 시 덮어쓰지 말고 원본을 백업 키로 보존한 뒤 오류를 표시한다

### 비통신 (요구사항이다)
- **CDN 참조 금지.** 폰트는 `public/fonts/`에 자체 호스팅
- **분석·크래시 리포팅·텔레메트리 금지.** 외부 API 호출 금지

### 색과 에셋
- 색은 `src/data/palette.ts`에서만. **Tailwind 기본 팔레트를 쓰지 마라**
- 팔레트는 **세계 32색 + 캐릭터 8색 = 40색.** `CHAR` 그룹은 캐릭터 팩 원본이므로 수정 금지
- **캐릭터 팩을 팔레트 리맵하지 마라.** 16px 8색이라 뭉갠다. 리맵 스크립트가 `characters/`를 건너뛴다
- 스프라이트 시트 값: **Kenney 타일셋 `spacing: 1`, 캐릭터 팩 `spacing: 0`.** 공유하지 마라
- 캐릭터 시트의 투명 픽셀은 마젠타에 알파 0이다. **알파를 평탄화하지 마라** — 분홍이 드러난다
- 파일 경로를 코드에 박지 마라. 에셋 매니페스트를 거친다. `path`가 `null`이면 플레이스홀더를 그리고 게임은 정상 작동해야 한다

### 수정 금지 파일
아래는 완성본이다. **읽기만 하고 수정하지 마라.** "개선"하지 마라.
```
src/data/palette.ts
src/data/characters.ts
src/data/content/companion-dialogue.ts
src/data/content/patron-dialogue.ts
src/data/content/dialogue-events.ts
src/data/content/region-text.ts
src/data/content/world-content.ts
```
대사와 서술은 문체 규약(§15)에 맞춰 작성됐다. 새로 쓰거나 다듬으면 톤이 무너진다.
텍스트가 필요한 곳에서는 반드시 이 파일들에서 가져온다.

### 관계 시스템의 방향 (자주 틀리는 지점)
- **교류 버튼이 없다.** 플레이어가 인물을 찾아가 호감을 올리는 구조가 아니다
- 호감은 동행 탐사·고향 지역 탐사·선물·대화 사건에서만 오른다
- 문턱을 넘으면 `pendingApproach`에 등록되고, **다음에 마을에 들어설 때 인물이 이쪽으로 걸어와 말을 건다**
- **고백은 인물이 한다.** 플레이어가 고백하는 선택지를 만들지 마라. 응답은 수락·보류·거절
- 연인은 여러 명 가능하고 질투 페널티는 없다. 대신 소개 연쇄(신규 인물 유입)는 우애 트랙 전용이다

### 문체
- **연대기·판정 결과·시대 전환**: 무주어 문어체, 과거형(`~했다`). 2인칭(`그대`·`당신`) 금지
- **인물 대화**: 현대 구어체. 호감 단계에 따라 말투가 변한다(존댓말 → 반말 → 연인 호칭)
- **UI 라벨**: 명사형·동사원형
- 오류 메시지는 무엇을 하면 되는지 명시한다

### 모바일
- 기준 기기는 **갤럭시 Z 플립 4** 메인 화면. 약 412×1006dp 세로 (2640×1080, DPR 2.625)
- 상단 HUD 40 고정 / 조작 180~240 (화면이 길면 자람) / 필드는 남는 높이를 흡수
- 필드에 최소 높이를 걸지 마라. 낮은 화면에서 조작부가 밖으로 밀려 D패드가 잘린다
- `env(safe-area-inset-*)` 를 지켜라. 플립4는 위에 펀치홀, 아래에 제스처 바가 있다
- 기획서 §0 은 아직 393×852 로 적혀 있다. 실제 기준은 위쪽이다
- 그리드 4방향 이동, 한 칸 140ms. 정지 상태에서 다른 방향 첫 입력은 **방향만 바꾼다**
- 타일 16px 원본 ×2 = 32px 렌더. 캐릭터 16×24 → 32×48
- 터치 타깃 최소 44dp
- `prefers-reduced-motion` 존중

### 결정된 사항
`docs/game-design-doc.md` §16의 결정 20개는 **전부 확정**이다. 임의로 바꾸지 마라.
바꿔야 할 이유가 생기면 먼저 말하고 승인을 받는다.

## 파일 지도

```
docs/game-design-doc.md    기획서. 지시받은 절만 읽는다
docs/quick-blocks.md       마일스톤 지시 순서
docs/ASSETS.md             에셋 조달과 라이선스
src/data/                  수정 금지 (위 목록)
raw-assets/                내려받은 원본. 읽기 전용
scripts/remap-palette.ts   빌드 전 실행. 런타임 코드 아님
```

## 커밋

마일스톤이 끝나면 커밋한다. `feat(M3): 마을 성장과 주 엔진` 형태로.
`*.keystore`, `*.jks`, `.env`는 절대 커밋하지 않는다.
